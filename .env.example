# Environment template for route-kun
# Week 1 runs with MSW mocks; Week 2 からは実キー/URL が必須になります。

# Runtime
NODE_ENV=development

# Client-side (public) keys
# Mapbox token is used in the browser. Restrict by domain in Mapbox.
NEXT_PUBLIC_MAPBOX_TOKEN=pk.test-route-kun

# Server-side keys
# Google Maps API key is used only on the server for distance/geocode.
GOOGLE_MAPS_API_KEY=AIzaYourGoogleMapsKey

# Supabase project
# URL and anon key may be used client-side (protected by RLS). Service role is server-only.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Database (optional in Week 1 if fully mocked)
DATABASE_URL=

# Optimizer service (Python/OR-Tools) endpoint
# For Week 1, leave empty or point to a mock.
OPTIMIZER_SERVICE_URL=http://localhost:8001

# E2E base URL (Playwright)
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000

# Notes:
# - Week 2 では Mapbox/Google/Optimizer の値が未設定だとアプリが起動しません。
# - 実運用の値は `.env.local`（コミットしない）に上書きしてください。
